<% title "Ad Targeting System" %>

<%= form_for(@keyword) do |f| %>
  <% if @keyword.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@keyword.errors.count, "error") %> prohibited this keyword from being saved:</h2>

      <ul>
      <% @keyword.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  First name: <%= f.text_field :name %><br />

  States : <%= f.select :states,
                        region_options_for_select(Carmen::Country.coded('US').subregions),
                        {prompt: 'Please select US States'},
                        { multiple:true, size: 20} %><br />

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
